import 'package:flutter/material.dart';

class DargTargetWidget extends StatelessWidget {
  const DargTargetWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: 
    Column(children: [
     
    ],),);
  }
}